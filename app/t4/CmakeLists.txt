PROJECT(NEWHELLO)
set(PROJECT_INSTALL_DIR "${PROJECT_BINARY_DIR}/../../../tmp")

# 设置头文件搜索路径
set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -I${PROJECT_INSTALL_DIR}/include -L${PROJECT_INSTALL_DIR}/lib")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -I${PROJECT_INSTALL_DIR}/include")
set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -L${PROJECT_INSTALL_DIR}/lib")

ADD_SUBDIRECTORY(src)